from .base_metric import BaseMetric
from typing import Dict, Any

class SafetyMetric(BaseMetric):
    """
    Abstract base class for safety metrics.
    This class defines the interface for safety metrics that can be applied to LLM responses.
    """
    def calculate(self, llm_response: str, **kwargs) -> Dict[str, Any]:
        """
        Calculates safety-specific metrics for the LLM response.
        
        Args:
            llm_response (str): The response generated by the LLM.
            **kwargs: Additional arguments specific to the metric.
        
        Returns:
            Dict[str, Any]: A dictionary containing calculated safety metrics.
        """
        raise NotImplementedError("Subclasses must implement this method.")